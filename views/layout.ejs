<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Super Suplementos - Loja Oficial' %></title>
    <style>
        :root {
            --primary: #FFCA05;
            --light: #E6E6E6;
            --gray: #737373;
            --dark: #000000;
            --white: #FFFFFF;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background: var(--white);
            color: var(--dark);
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .header {
            background: var(--dark);
            border-bottom: 3px solid var(--primary);
        }

        .header-top {
            background: var(--gray);
            color: var(--white);
            padding: 8px 0;
            font-size: 14px;
        }

        .header-main {
            padding: 15px 0;
        }

        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 30px;
        }

        .logo {
            font-size: 28px;
            font-weight: bold;
            color: var(--primary);
            text-decoration: none;
        }

        .logo span {
            color: var(--white);
        }

        .search-box {
            flex: 1;
            max-width: 500px;
        }

        .search-box input {
            width: 100%;
            padding: 12px 20px;
            border: none;
            border-radius: 25px;
        }

        .user-actions {
            display: flex;
            align-items: center;
            gap: 20px;
            color: var(--white);
        }

        .user-actions a {
            color: var(--white);
            text-decoration: none;
        }

        .nav {
            background: var(--dark);
            border-top: 1px solid var(--gray);
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        .nav-links a {
            color: var(--white);
            text-decoration: none;
            padding: 15px 0;
            display: block;
        }

        .nav-links a:hover {
            color: var(--primary);
        }

        .hero {
            background: linear-gradient(135deg, var(--dark) 0%, var(--gray) 100%);
            color: var(--white);
            padding: 80px 0;
            text-align: center;
        }

        .hero h1 {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .hero p {
            font-size: 20px;
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            background: var(--primary);
            color: var(--dark);
            padding: 15px 30px;
            border: none;
            border-radius: 5px;
            font-weight: bold;
            text-decoration: none;
            cursor: pointer;
        }

        .btn:hover {
            background: #e6b800;
        }

        .section {
            padding: 80px 0;
        }

        .section-title {
            text-align: center;
            font-size: 36px;
            margin-bottom: 50px;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .product-card {
            background: var(--white);
            border: 1px solid var(--light);
            border-radius: 10px;
            padding: 20px;
            transition: transform 0.3s;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-image {
            width: 100%;
            height: 200px;
            background: var(--light);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
            margin-bottom: 15px;
        }

        .product-name {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .product-brand {
            color: var(--gray);
            margin-bottom: 10px;
        }

        .product-price {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 15px;
        }

        .product-actions {
            display: flex;
            gap: 10px;
        }

        .btn-small {
            padding: 10px 20px;
            font-size: 14px;
        }

        .footer {
            background: var(--dark);
            color: var(--white);
            padding: 50px 0 20px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-section h3 {
            color: var(--primary);
            margin-bottom: 20px;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 10px;
        }

        .footer-links a {
            color: var(--light);
            text-decoration: none;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid var(--gray);
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 15px;
            }
            
            .nav-links {
                flex-wrap: wrap;
                gap: 15px;
            }
            
            .hero h1 {
                font-size: 36px;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="header-top">
            <div class="container">
                <div style="display: flex; justify-content: space-between;">
                    <div>🚚 Frete Grátis acima de R$ 199</div>
                    <div>
                        <a href="#" style="color: var(--white); margin-left: 15px;">Minha Conta</a>
                        <a href="#" style="color: var(--white); margin-left: 15px;">Ajuda</a>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="header-main">
            <div class="container">
                <div class="header-content">
                    <a href="/" class="logo">SUPER<span>SUPLEMENTOS</span></a>
                    
                    <div class="search-box">
                        <input type="text" placeholder="Buscar produtos...">
                    </div>
                    
                    <div class="user-actions">
                        <a href="#">👤 Entrar</a>
                        <a href="#">❤️ Favoritos</a>
                        <a href="#">🛒 Carrinho</a>
                    </div>
                </div>
            </div>
        </div>
        
        <nav class="nav">
            <div class="container">
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/produtos">Produtos</a></li>
                    <% categorias.forEach(categoria => { %>
                        <li>
                            <a href="/produtos?categoria=<%= categoria %>">
                                <%= categoria.charAt(0).toUpperCase() + categoria.slice(1) %>
                            </a>
                        </li>
                    <% }); %>
                    <li><a href="/checkout">Checkout</a></li>
                </ul>
            </div>
        </nav>
    </header>

    <main>
        <%- content %>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>SUPER SUPLEMENTOS</h3>
                    <p>Os melhores suplementos com preços imbatíveis e qualidade garantida.</p>
                </div>
                
                <div class="footer-section">
                    <h3>PRODUTOS</h3>
                    <ul class="footer-links">
                        <% categorias.forEach(categoria => { %>
                            <li>
                                <a href="/produtos?categoria=<%= categoria %>">
                                    <%= categoria.charAt(0).toUpperCase() + categoria.slice(1) %>
                                </a>
                            </li>
                        <% }); %>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>ATENDIMENTO</h3>
                    <ul class="footer-links">
                        <li><a href="#">Como Comprar</a></li>
                        <li><a href="#">Formas de Pagamento</a></li>
                        <li><a href="#">Entrega</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>CONTATO</h3>
                    <p>📞 (11) 9999-9999</p>
                    <p>✉️ contato@supersuplementos.com.br</p>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2024 Super Suplementos. Todos os direitos reservados.</p>
            </div>
        </div>
    </footer>
</body>
</html>